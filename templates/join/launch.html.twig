{% extends 'popup.html.twig' %}

{% block title %}{{ 'join.register_title'|trans }}{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path ('register') }}" class="nav-link text-yellow">
            <h4>{{ 'join.register_here'|trans }}</h4>
        </a>
        <div class="container">
            <a class="nav-link text-yellow" href=" {{ path ('register') }}">
            {{ 'join.register_info'|trans|raw }}
            </a>
        </div>
        <div class="container">
            <btn class="danger">
                <a href="https://www.helloasso.com/associations/binioufous/adhesions/cotisations" class="green-hover text-yellow"><h3>{{ 'join.subscribe_here'|trans }}</a></h3>
            </btn>
        </div>
    </div>
{% endblock %}
