<table class="table table-hover text-center">
            <h3>{{ 'desk.list.simples_title'|trans }}</h3>
            <thead>
                <tr>
                    <th>{{ 'Name'|trans }}</th>
                    <th>{{ 'Picture'|trans }}</th>
                    <th>{{ 'Country'|trans }}</th>
                    <th>{{ 'City'|trans }}</th>
                    <th>{{ 'Instrument'|trans }}</th>
                    <th>{{ 'Actions'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for simple in simples %}
                <tr>
                    <td>{% if simple.gender == 'male' %}<i class="ri-men-fill" aria-hidden="true"></i>{% else %}<i class="ri-women-fill" aria-hidden="true"></i>{% endif %} {{ simple.fullName }} </td>
                    <td>{% if simple.picture == null %}
                        <i class="ri-image-line" aria-hidden="true"></i> <em>{{ 'no picture'|trans }}</em>
                    {% else %}
                        <img src="{{ asset('uploads/pictures/' ~ simple.picture) }}" class="items-align-right avatar avatar-medium" alt=" Avatar de {{ simple.firstname }}">
                    {% endif %}</td>
                    <td>{{ simple.country }}</td>
                    <td>{{ simple.city }}</td>
                    <td>{{ simple.instrument.title }}</td>
                    <td><a href="{{ path ('user_show', {slug: simple.slug}) }}" class="btn btn-green"><i class="ri-edit-line"></i></a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
