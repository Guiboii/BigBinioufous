<table class="table table-hover">
            <h3>Les simples utilisateurs</h3>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Picture</th>
                    <th>Gender</th>
                    <th>City</th>
                    <th>Instrument</th>
                    <th>Wished role</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                <tr>
                    <td>{{ user.fullName }}</td>
                    <td><img src="{{ asset('uploads/pictures/' ~ user.picture) }}" class="items-align-right avatar avatar-medium" alt="Avatar de {{ user.fullName}}"> </td>
                    <td>{{ user.gender }}</td>
                    <td>{{ user.city }}</td>
                    <td>instrument</td>
                    <td>{{ user.wish }}</td>
                    <td> <a href="{{ path ('user_valid', {'slug' : user.slug, 'wish' : user.wish})}}" class="btn btn-primary"><i class="fas fa-check"></i></a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>