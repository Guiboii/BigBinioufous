{% extends 'desk/base.html.twig' %}

{% block title %}{{ 'desk.change_password'|trans }}{% endblock %}

{% block body %}

<div class="container">
    <h1>{{ 'desk.change_password'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-green">{{ 'desk.update_submit'|trans }}</button>
    {{ form_end(form) }}
</div>
{% endblock %}
