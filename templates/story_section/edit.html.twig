{% extends 'desk/base.html.twig' %}

{% block title %}{{ 'story_admin.edit_title'|trans }}{% endblock %}

{% block importmap %}{{ importmap(['app', 'story-admin']) }}{% endblock %}

{% block body %}
<div class="container mt-2">
    <h1>{{ 'story_admin.edit_heading'|trans }}</h1>

    {{ include('story_section/_form.html.twig', {button_label: 'story_admin.update_submit'}) }}

    <a href="{{ path('story_section_index') }}" class="text-green">{{ 'story_admin.back_to_list'|trans }}</a>
</div>
{% endblock %}
