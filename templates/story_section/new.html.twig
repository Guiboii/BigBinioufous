{% extends 'desk/base.html.twig' %}

{% block title %}{{ 'story_admin.new_title'|trans }}{% endblock %}

{% block importmap %}{{ importmap(['app', 'story-admin']) }}{% endblock %}

{% block body %}
<div class="container mt-2">
    <h1>{{ 'story_admin.create_heading'|trans }}</h1>

    {{ include('story_section/_form.html.twig') }}

    <a href="{{ path('story_section_index') }}" class="text-green">{{ 'story_admin.back_to_list'|trans }}</a>
</div>
{% endblock %}
