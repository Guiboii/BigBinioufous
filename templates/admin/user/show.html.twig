{% extends 'desk/base.html.twig' %}

{% block title %}{{ user.fullname }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <a class="btn btn-primary" href="{{ path ('admin') }}">Retour aux listes</a>
            <div class="col text-center">
                <div class="card-header text-center">
                    <h1>{{ user.fullName }}</h1>
                </div>
            </div>
            <button class="btn btn-primary">Contacter</button>
        </div>
        <div class="container text-center">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Role demandé</th>
                    </tr>
                </thead>
                <tbody>
                    
                    <tr>
                        <td>{{ user.wish }}</td>
                        <td><a href="#" class="btn btn-primary"><i class="far fa-check-circle"></i></a>
                        <a href="#" class="btn btn-danger"><i class="fas fa-ban"></i></a></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
