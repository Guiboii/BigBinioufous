security:
    password_hashers:
        App\Entity\User:
            algorithm: bcrypt
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        users_in_memory: { memory: null }
        in_database:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            provider: in_database

            form_login:
                login_path: join
                check_path : join
                default_target_path : desk

            logout:
                path: logout
                target: home

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/accountant, roles: ROLE_COMPTA }
        # Étoiler un document (préférence perso) : ouvert à tout membre,
        # règle plus spécifique donc placée avant la restriction générale
        # ci-dessous sur /desk/music/documents (premier match qui gagne).
        - { path: ^/desk/music/documents/[^/]+/favorite, roles: ROLE_USER }
        # Glisser-déposer/suppression (morceaux, documents, dossiers) sur
        # /desk/music : réservé aux binioufous, pas juste ROLE_USER.
        - { path: ^/desk/music/documents, roles: ROLE_BINIOUFOUS }
        - { path: ^/desk/music/folders, roles: ROLE_BINIOUFOUS }
        - { path: ^/desk, roles: ROLE_USER }
        # Gestion des morceaux/voix (écriture) réservée aux admins ; la liste
        # et la fiche d'un morceau (GET) restent publiques, non concernées.
        - { path: ^/music/new, roles: ROLE_ADMIN }
        - { path: ^/music/quick-upload, roles: ROLE_BINIOUFOUS }
        - { path: ^/music/[^/]+/edit, roles: ROLE_ADMIN }
        - { path: ^/music/[^/]+/voice, roles: ROLE_ADMIN }
        - { path: ^/music, methods: [DELETE], roles: ROLE_ADMIN }
