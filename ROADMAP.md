# Roadmap

Objectif : remettre BigBinioufous dans un état correct pour 2026, et facile à faire évoluer ensuite.

Ordre choisi pour éviter de refaire le même travail deux fois : les fondations et l'architecture d'abord, le contenu (pages, fonctionnalités) ensuite.

## Phase 1 : Fondations

- [x] Conventions de code : `.editorconfig`, PHP-CS-Fixer (`@Symfony`), Twig-CS-Fixer, ESLint/Prettier (flat config), `Makefile` (`make lint` / `make lint-fix`)
- [x] Intégrer ces conventions à la CI (`.github/workflows/pipeline.yml`, consolidé avec la CI sécurité JS : lint → sécurité, jobs enchaînés via `needs:`)

## Phase 2 : Upgrade Symfony 5 → 7.4 (prérequis pour AssetMapper)

**Bloquant découvert le 2026-08-05** : `symfony/asset-mapper` n'existe qu'à partir de Symfony `6.3.0`, le projet tournait en `5.4.45`. Upgrade fait avant de reprendre le pivot AssetMapper.

- [x] Upgrade composer.json 5.* → 6.4.* (aucun conflit de dépendances remonté par composer)
- [x] Remplacer `sensio/framework-extra-bundle` par les attributs PHP natifs Symfony 6.2+ (`#[Route]` sur tous les contrôleurs)
- [x] Mapping Doctrine ORM : annotations → attributs PHP (`#[ORM\Entity]` etc.) sur les 6 entités
- [x] `security.yaml` : `anonymous` (obsolète) retiré, `encoders` → `password_hashers`
- [x] `User.php` : interface de sécurité modernisée (`getRoles(): array`, `getUserIdentifier()`, `PasswordAuthenticatedUserInterface`)
- [x] `Kernel.php` : `RouteCollectionBuilder` (supprimé) → `RoutingConfigurator`
- [x] Bump PHP minimum requis par composer.json à `^8.1` (Symfony 6.3+), couvert par le PHP CLI système en 8.4
- [x] Toutes les pages testées manuellement (accueil, story, schedule, join, login, music, desk, admin, accountant) + validé par l'utilisatrice en localhost
- [x] Upgrade vers Symfony 7.4 LTS : aucun changement de code nécessaire (tout traité au passage 6.4), seul `symfony/webpack-encore-bundle` bumpé `^1.7` → `^2.4` pour supporter `symfony/asset ^7.0`

## Phase 2bis : Pivot AssetMapper (après la Phase 2)

- [x] Remplacer Webpack Encore par AssetMapper (natif Symfony, zéro build Node en prod) : `webpack-encore-bundle`, `webpack.config.js` supprimés, dépendances JS déclarées dans `importmap.php`
- [x] `eternicode/bootstrap-datepicker` retiré (confirmé mort), ce qui a aussi supprimé `robloach/component-installer` (abandonné) et `components/`
- [x] Adapter les 9 templates Twig qui référencent les assets compilés (`encore_entry_*` → `importmap()`)
- [x] Piège Bootstrap 5/Three.js récent résolus par défaut par `importmap:require` (incompatibles avec le code existant) : versions épinglées explicitement (`bootstrap@^4.6`, `three@0.128.0`)
- [x] Piège des URLs hashées pour les fichiers binaires (`.gltf`, images) référencés en JS : résolu via `window.BB_ASSETS` injecté dans `base.html.twig`
- [x] Testé manuellement (curl + navigateur Playwright, screenshot de la mascotte 3D) sur toutes les pages, 0 erreur console (hors CDN wavesurfer bloqué par la sandbox de test, sans rapport avec la migration)
- [x] CI JS réévaluée : gardée telle quelle, `npm audit` passe à 0 vulnérabilité (devDependencies de lint uniquement désormais)

## Phase 3 : Déploiement automatique (VPS auto-géré)

- [x] Approche décidée : déploiement direct (pas de Docker). Priorité donnée à la simplicité de reprise en main par un·e futur·e mainteneur·se de l'assos qui ne connaît pas forcément Docker, plutôt qu'à la reproductibilité de l'environnement (peu utile ici : un seul VPS, pas de scaling prévu)
- [x] Pipeline GitHub Actions : job `deploy` ajouté (`needs: [composer-audit, npm-audit]`, déclenché sur push vers `master` uniquement) : connexion SSH au VPS, `git pull`, `composer install --no-dev`, migrations, `asset-map:compile`, `cache:clear`
- [x] Migrations Doctrine en prod : automatisées comme étape du pipeline (`doctrine:migrations:migrate --no-interaction`, jouée après le `git pull` et avant le `cache:clear`), pas d'étape manuelle à retenir pour un futur repreneur
- [ ] Secrets de déploiement à créer dans GitHub Secrets (Settings → Secrets and variables → Actions) : `VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY` (clé privée), `VPS_PATH` (chemin du repo cloné sur le VPS). Étape manuelle côté GitHub, à faire par l'utilisatrice.
- [ ] Setup initial one-shot sur le VPS (avant le premier déploiement automatique) : `git clone` du repo, PHP 8.1+/MySQL/Apache ou Nginx installés, `.env.prod.local` créé à la main (voir point suivant), `composer install` une première fois
- [ ] Gestion `.env` dev vs prod : `.env` reste les valeurs par défaut (déjà le cas), `.env.local`/`.env.prod.local` pour les vraies valeurs de prod (jamais commitées, créé une fois à la main sur le VPS), voir `composer dump-env prod` pour compiler les variables en prod

## Phase 4 : Système de traduction (i18n)

Priorité moyenne, mais à faire *avant* le passage page par page (Phase 5) : évite de retraduire/re-toucher chaque page une deuxième fois après coup.

- [x] Mettre en place le composant de traduction Symfony (`symfony/translation`, déjà en dépendance) : `default_locale: fr`, `enabled_locales: [fr, en, br]`, fallback `fr`
- [x] Extraire les textes en dur des templates Twig (tous) et des labels/placeholders des formulaires (`src/Form/*.php`, via `ApplicationType::getConfiguration` qui passe désormais par le `TranslatorInterface`) vers `translations/messages.{fr,en,br}.yaml` et `translations/validators.{fr,en,br}.yaml`
- [x] Langues couvertes : **fr** (défaut), **en**, et **br** (breton, "pour le fun" ; traduction perso non relue par un·e locuteur·rice native, à vérifier si l'exactitude compte un jour). Voir [doc/i18n.md](doc/i18n.md) pour le détail (sélecteur de langue, route `/locale/{locale}`, limite du check CI sur `br`)

## Phase 5 : Passage page par page (code + UX)

- [x] Navbar principale (`templates/partials/header.html.twig`) : contraste rouge/vert historique corrigé (~1.1:1 → conforme WCAG AA), palette clarifiée (vert clair/foncé + or existants, rouge retiré du chrome interactif), icônes Remixicon, `<select>` natif remplacé par des liens (moins de mélange chrome Bootstrap/custom), icônes seules sur mobile (texte gardé en `sr-only` pour l'accessibilité)
- [x] Une page à la fois : lisibilité du code (Twig/PHP/JS) et lisibilité visuelle/UX
- [x] Ordre des pages : vitrine d'abord, en commençant par **Histoire** (`templates/story/`, branche `phase5_story`)
- [x] Minisite Histoire (`templates/story/minisite.html.twig`, `assets/story/minisite.css`) : iframe morte (ancien domaine `4fun.binioufous.fr` mort en DNS) corrigée, refonte complète en page autonome (fini le "page dans la page" avec navbar/barre Symfony dupliquées), thème terminal rétro vert biniou (IBM Plex Mono + VT323, contrastes vérifiés AA/AAA + simulation daltonisme), cadre recalé en `vw`/`vh` sur l'écran 3D de `/story`. Chrome "bureau" ajouté par-dessus : fenêtre terminal à 80% de l'écran flottant sur un bureau (dégradé + icônes CSS + barre des tâches avec horloge/batterie décorative), scrollbar thématisée. `<li>`/ids dupliqués/menu d'ancres mort corrigés au passage.
- [x] Adaptation mobile de la page Histoire : sous 700px, `/story` redirige directement vers `/story/mini` plutôt que de charger la scène 3D (illisible en petit + gaspillage data/batterie pour rien). La minisite y bascule en plein écran avec une petite navbar de site (Accueil/Musique/Planning/Rejoindre) + une note "vue simplifiée" (`story.mobile_note`, fr/en/br), visibles uniquement hors iframe (détection `window.self !== window.top`) pour ne pas revenir au bug "page dans la page".
- [x] Audit accessibilité global (2026-08-10), sur les 7 pages accessibles sans connexion (`/`, `/join`, `/login`, `/register`, `/story`+`/story/mini`, `/schedule`, `/music`+`/music/{id}`) : `h1` unique ajouté sur les pages qui n'en avaient aucun (Accueil, Musique) ou une hiérarchie qui sautait des niveaux (Planning, `h4`/`h5` directement) ; scènes 3D purement décoratives passées en `aria-hidden` (la vraie navigation reste dans la navbar, atteignable au clavier, la scène 3D cliquable à la souris n'a jamais eu d'équivalent clavier) ; skip-link (`.skip-link` dans `assets/main/app.css`, `#main-content` dans `base.html.twig`) ; menu mobile de la navbar fermable à l'Échap (`assets/main/app.js`) ; boutons du lecteur Musique (`#stopTrack`/`#playPause`/`#loopRegion`) qui étaient des `<div>` cliquables (ni focusables ni activables au clavier) convertis en vrais `<button>` ; `button:focus { outline: none; }` du lecteur (sans alternative) remplacé par `:focus-visible` ; contrastes recalculés et corrigés là où c'était sous AA (`.text-green`/`.text-redwall` sur `/register`, 2.77:1/3.23:1 → 7.15:1 AAA / 4.56:1 AA) ; `alt`/`aria-label`/`aria-live`/`role="dialog"` ajoutés au cas par cas (affiches d'événements, mini-calendrier, popups). Détail complet par page dans le rapport de session, pas dupliqué ici.
    - **Bug structurel découvert au passage sur `/join`, pas juste esthétique** : `join/login.html.twig` et `join/launch.html.twig` faisaient `{% extends 'popup.html.twig' %}` (document HTML complet avec son propre `<head>`/`<script type="importmap">`) tout en étant `{{ include() }}`-ées **inline** dans `join/index.html.twig` (qui étend déjà `base.html.twig`, un autre document complet) : 2-3 `<html><head>` imbriqués dans la même page, dont 2-3 `<script type="importmap">`. Un document HTML n'accepte qu'un seul importmap valide : selon l'ordre de résolution du navigateur, celui réellement utilisé pouvait être incomplet, cassant le chargement du CSS de la page (texte devenu invisible, boutons non cliquables) de façon intermittente et difficile à reproduire (`ctrl+F5` la révélait, un rechargement normal avec cache navigateur pouvait la masquer). `login.html.twig`/`launch.html.twig` transformés en simples fragments (plus d'extends), bouton de fermeture réintégré directement dedans. Effet de bord : la route `/login` (`LoginController`), qui rendait `join/login.html.twig` en document autonome via ce même mécanisme, ne pouvait plus fonctionner telle quelle une fois le fragment allégé ; elle ne servait de toute façon à rien (`security.yaml` pointe `login_path`/`check_path` vers `join`, jamais vers `login`, la vraie connexion vit déjà sur `/join`) donc transformée en redirection vers `/join` plutôt que ressuscitée.
    - Bug préexistant sans rapport trouvé en vérifiant `/` après coup : `mascotte.js` référence `window.BB_ASSETS['schedule/flyer002.png']` (texture d'affiche sur le meuble 3D) depuis le pivot AssetMapper du 2026-08-05, jamais ajoutée à la liste `window.BB_ASSETS` de `base.html.twig` (piège documenté dans la section Frontend de `CLAUDE.md`, raté pour ce fichier précis) : `TextureLoader().load(undefined)` → requête vers littéralement `/undefined`, texture jamais affichée. Clé ajoutée.
    - **Non fait, à reprendre** : cadrage de l'overlay du calendrier `/schedule` toujours en vw/%, jamais remesuré ni recalé en vraie projection 3D→2D (cf. section dédiée de `CLAUDE.md`) ; `#waveform` (wavesurfer.js, page Musique) reste un widget non opérable au clavier (pas de seek clavier), gap documenté mais pas corrigé.
- [x] Musique (`templates/music/index.html.twig`, `assets/music/music.css`, branche `phase5_music`) : wavesurfer.js épinglé à `6.6.4` (chargeait la 7.x sans version, API différente, le lecteur ne fonctionnait pas du tout : pas de waveform, play/pause/stop/boucle tous morts). Le clic sur un morceau charge bien la piste dans le lecteur (`wavesurfer.load(...)` existe dans `Player.js`, contrairement à ce qu'on pensait au premier coup d'œil) : ça plantait juste avant, `Player.js` charge la 1ère piste automatiquement à l'ouverture sans vérifier que la playlist n'est pas vide. Contraste des icônes du lecteur corrigé (`--bb-gold-pale` à la place de `rgba(0,0,0,0.4)`/`--bb-green-mid`/`--bb-red-dark`/`--bb-ink`, tous illisibles sur le fond `#695044`, ~1.1-1.7:1). Formulaire de connexion embarqué retiré (n'avait rien à faire là), remplacé par un simple message + lien vers `/join`, clés de traduction `login_email_placeholder`/`login_password_placeholder`/`unlock`/`register` devenues inutiles supprimées en fr/en/br.
- [x] Catégories "Disco"/"Rock" bidons retirées : `Track` n'a aucun champ genre, les deux boucles Twig itéraient sur la même liste `tracks` en entier (doublons garantis, et la 2e catégorie avait même un lien codé en dur vers un fichier de test). Remplacées par une seule vraie liste (`.playlist`). Traductions `category_disco`/`category_rock` devenues inutiles supprimées en fr/en/br. Si une vraie catégorisation par genre est voulue un jour : ajouter un champ à `Track` + migration + formulaire, pas juste refaire deux listes en dur.
- [x] `.loggedinContent` (liens de téléchargement membres) : `d-none` codé en dur retiré, le `{% if app.user %}` gère déjà la visibilité, c'était redondant et faux (invisible même connecté·e avant). Vérifié par lecture du code à l'époque, pas en conditions réelles connectées (la page `/login` de l'époque n'avait pas de vraie balise `<form>`, cf. entrée "Audit accessibilité global" pour la résolution).
- [x] `Player.js` protégé contre une playlist vide (`if (links.length)` avant de charger la 1ère piste / de passer à la suivante) : avant, plantait au chargement dès qu'il n'y avait aucun morceau en base.
- [x] Layout de `.playlist-wrap` refait : 1 seule scrollbar (sur `.playlist`) à la place de 3 imbriquées (lecteur + accordéon + panneau membre), plus de respiration (`height: 237px` → `280px`), texte du lecteur et de la playlist en `--bb-gold-pale` (fond marron très sombre partout, texte hérité noir/vert illisible sinon), scrollbar fine dorée plutôt que la barre grise par défaut. Icône morte `glyphicon glyphicon-play` (Bootstrap 3, n'existe plus depuis Bootstrap 4) remplacée par le même SVG `bi-play-fill` que les boutons du lecteur.
- [x] Aucun morceau de démo n'était réellement jouable : `AppFixtures.php` ne remplissait jamais `trackFilename` (vide sur les 10 morceaux créés), et même corrigé le lien pointait quand même au mauvais endroit (`href="{{ track.trackFilename }}"` = juste le nom de fichier, résolu relatif à `/music/` au lieu de `/uploads/music/`, cf. `services.yaml` paramètre `mp3`). Les deux corrigés : fixtures pointent vers les 2 fichiers mp3 réels déjà présents dans `public/uploads/music/` (`Oi-Tate-A2.mp3`, `test.mpga`), lien en `asset('uploads/music/' ~ track.trackFilename)` (même convention que les photos de profil). Vérifié : waveform réelle affichée, lecture/pause fonctionnels.
- [x] Cadre du lecteur Musique recalé par vraie projection 3D->2D (`Vector3.project(camera)`, points d'ancrage mesurés par raycast sur le modèle) plutôt qu'en `vw`/`vh` : le vw/vh cassait dès que le ratio largeur/hauteur de la fenêtre s'écartait de la taille de calibrage (caméra en perspective, FOV horizontal dépendant du ratio contrairement au vertical). Voir section "Cadrage d'un overlay 2D..." de `CLAUDE.md`.
- [x] Adaptation mobile de la page Musique : même pattern qu'Histoire (`.is-mobile` posée avant le 1er rendu, scène 3D pas initialisée en dessous de 700px), mais sans redirection vers une autre page cette fois (le lecteur/la playlist existent déjà en DOM normal, pas besoin d'un template "minisite" séparé). Bannière "vue simplifiée" (`music.mobile_note`, fr/en/br : traduction bretonne reprise telle quelle de `story.mobile_note`, pas de locuteur pour en vérifier une propre à Musique).
- [x] Planning, ménage de dette technique (`templates/schedule/`, `assets/schedule/schedule.css` nouveau, branche `phase5_schedule`) : CSS déménagé d'`app.css` vers un fichier dédié (mêmes conventions que Musique/Histoire). Layout refait en `flex`/`vw` centré et responsive à la place du `position:absolute` en px fixes (`margin: 56px 150px`) qui ne suivait pas la fenêtre. Scrollbar par défaut du navigateur remplacée par une scrollbar fine thématisée. Code mort retiré : mesh `flyerRight` jamais ajouté à la scène, branche de raycast testant un objet `'disco'` qui n'existe dans aucune scène ici, entrée `BB_ASSETS` orpheline. Adaptation mobile ajoutée (même pattern que Musique/Histoire, la scène 3D ici est de toute façon peu utilisée). **Piège découvert en le corrigeant** : `#calendar` réutilisait la classe Bootstrap générique `.navbar` juste pour le style des pills, et héritait donc du `position:fixed` de la vraie navbar du site (`assets/main/app.css`, seule autre utilisation de `.navbar` dans tout le projet), sortait du flux et flottait n'importe où selon le calcul de repli du navigateur pour un `position:fixed` sans `top`/`left` (même piège que `.navbar-toggler` documenté dans `CLAUDE.md`). Corrigé par un `position: static` explicite sur `#calendar`.
- [x] Contenu réel du Planning (décidé le 2026-08-10) : entité `Event` (titre, lieu, type répétition/concert/autre, date, description facultative, affiche facultative), CRUD sous `/admin/event` (`EventController`, `EventType`, `EventRepository`), réservé `ROLE_ADMIN` via l'`access_control: ^/admin` déjà en place dans `security.yaml` (pas de vérif de rôle en plus à coder). Lien "Administration" du menu membre (`desk/partials/header.html.twig`), mort depuis toujours (`href="#"`), rebranché vers `/admin/event`. `/schedule` affiche les événements réels groupés par mois (une pastille de mois générée seulement pour les mois qui en ont vraiment, fini le lien mort). Palette recalée sur l'identité du site (rouge/vert/or plutôt que le blanc Bootstrap générique par défaut) + icônes Remixicon (même famille que la navbar) devant titre/date/adresse/agenda, à la demande de l'utilisatrice après un 1er jet trop générique. Adresse cliquable (lien Google Maps), bouton "Ajouter à mon agenda" générant un fichier `.ics` à la volée (`ScheduleController::ics()`, standard iCalendar, pas d'API externe/OAuth requis, s'importe dans Google/Outlook/Apple Calendar). Traductions fr/en complètes ; **pas de traduction bretonne pour ce gros lot de nouvelles clés** (vocabulaire from scratch, pas de locuteur pour valider, tombe en fallback fr, contrairement aux notes "vue simplifiée" réutilisées telles quelles d'une page à l'autre, celles-ci n'avaient rien à recopier).
- [x] 2 vrais bugs préexistants découverts en testant ce CRUD, sans rapport avec le Planning en soi mais qui bloquaient sa mise au point :
    - `doctrine:migrations:migrate` plantait dès le tout premier lancement sur une base fraîche : une migration de 2020 (`Version20201109153927`) faisait `DROP TABLE migration_versions` sans `IF EXISTS`, table qui n'existe jamais sur une base jamais migrée avec l'ancien nommage par défaut de Doctrine Migrations. Corrigé. Les autres migrations legacy (dérive de schéma préexistante sur `track`/`user`, sans rapport) n'ont volontairement pas été retouchées, pas le scope ici.
    - `http_method_override` désactivé dans `config/packages/framework.yaml` (commenté, défaut `false` sur cette version de Symfony) : cassait TOUS les formulaires de suppression du site utilisant le pattern `_method=DELETE` en champ caché (déjà utilisé par `music/_delete_form.html.twig`, probablement jamais fonctionnel). Activé.
    - **Outillage migrations toujours fragile après ces 2 correctifs** : `doctrine:migrations:diff`/`migrate`/`status` renvoient une erreur `MetadataStorageError` ("metadata storage is not up to date") sur cette base locale, pour une raison non identifiée (probablement un désaccord de comparaison de schéma entre cette version de Doctrine Migrations et MySQL 5.7, pas creusé plus). Contournement utilisé pour la suite (colonne `Event::$endDate`) : `ALTER TABLE` joué en direct via le client `mysql`, fichier de migration écrit à la main pour l'historique (`Version20260810142000`), ligne insérée manuellement dans `doctrine_migration_versions`. À déboguer proprement un jour plutôt que recontourner à chaque nouvelle colonne.
- [x] Vrai calendrier mois/jours (grille avec jours de la semaine, navigation flèche précédent/suivant vers n'importe quel mois, jours à événement en rouge cliquables) demandé par l'utilisatrice à la place des pastilles par mois, jugées "disparues" visuellement après le passage à la palette du site. Rendu en JS pur (`initMiniCalendar()` dans `schedule.js`, données passées en JSON via `<script type="application/json">`, pas de lib externe), tourne indépendamment du 3D comme `Player.js` sur Musique (fonctionne aussi en mode mobile). Ouvre par défaut sur le mois du jour, ou sur le premier mois à venir qui a des événements si le mois du jour est vide ; lit aussi `#month-XX` dans l'URL au chargement pour rouvrir directement dessus (lien partagé, retour arrière du navigateur).
- [x] Heure de fin facultative (`Event::$endDate`, nullable) : permet d'afficher "09:17-12:34" plutôt qu'une heure de fin à 2h par défaut inventée pour l'export agenda. Répercutée dans l'affichage carte, le `.ics` et les liens Google/Outlook (`ScheduleController::resolveEnd()`, repli sur +2h uniquement si `endDate` absent).
- [x] "Ajouter à mon agenda" étendu à 3 fournisseurs (Google, Outlook, .ics générique pour Apple Calendar/Thunderbird/autres) plutôt qu'un seul lien .ics, en petits boutons distincts avec icônes Remixicon (`ScheduleController::buildCalendarLinks()`, deep links publics Google/Outlook, pas d'API/OAuth). Icône Microsoft pleine (`ri-microsoft-fill`) écartée après retour utilisatrice (rendu comme un carré noir uniforme à cette taille) au profit de `ri-windows-line`, plus lisible en petit.
- [x] Description d'événement mise en valeur (encadré doré, bordure rouge, icône `ri-file-text-fill`) plutôt qu'une ligne de texte perdue parmi les autres, sur demande explicite ("faut que les gens voient la description").
- [x] 2 correctifs mobile trouvés en testant sur la nouvelle page : (a) `.scroll` gardait son `overflow-y:auto` + hauteur limitée du mode desktop, donc 2 scrolls imbriqués (celui de `.scroll` et celui de la page) peu lisibles sur petit écran ; un seul scroll (celui de la page) désormais, `.scroll` prend toute la hauteur de son contenu. (b) calendrier repassé avant la liste d'événements (`order:-1` en flex) : plus utile pour se repérer avant de dérouler la liste. Scrollbar native de la page (visible sur mobile, contrairement au desktop où le canvas 3D plein écran la masque) thématisée en doré/vert plutôt que la barre grise par défaut.
- [x] Rejoindre (`templates/join/`, `assets/login/join.css` nouveau, branche `phase5_join`) : CSS déménagé d'`app.css` vers un fichier dédié (même convention que Musique/Histoire/Planning). Bugs corrigés :
    - `.join` ("Rejoindre") en texte rouge posé directement sur le pan rouge du mur 3D (`join.js`) : ~1:1, invisible. `.login`/`.join` refaits en vrais boutons pilule avec fond propre (indépendant de la couleur du mur derrière), `.login` fond `--bb-gold`/texte `--bb-ink` (9.81:1), `.join` fond `--bb-red-dark`/texte `--bb-white` (8.56:1).
    - Largeur des boutons figée à `150px` en dur : la pilule de survol ne s'ajustait pas à la longueur réelle du texte, décalée selon la langue (fr/en/br). Passé en `width` auto + `padding`, se dimensionne sur le texte quelle que soit la langue.
    - `#joinForm`/`#loginForm` centrés en `margin-top/margin-left` px fixes calculés une fois pour une taille supposée (440px de large avec `margin-left:-170px` au lieu de `-220px`, 50px d'écart constant, en plus de dériver dès que le contenu change de hauteur). Passé en `transform: translate(-50%,-50%)`, se recalcule sur la taille réelle à chaque rendu.
    - Texte des popups en `--bb-gold` sur fond `--bb-red`/`--bb-green-mid` (règle globale `form{color:...}` + `.text-yellow` sur les liens) : 3.24:1, sous le minimum AA. Passé en blanc (6.07-6.82:1).
    - **Piège trouvé après un 1er correctif jugé insuffisant par l'utilisatrice** : au survol, le texte de `.join` redevenait vert (`--bb-green-mid` sur `--bb-red-dark`, ~1.26:1) à cause d'une règle globale `a:hover { color: var(--bb-green-mid) }` (`assets/main/app.css`) dont la spécificité (élément+pseudo-classe) dépasse une classe seule (`.login`/`.join`), le survol n'avait jamais réaffirmé sa propre couleur. Corrigé en réaffirmant `color` explicitement dans `.login:hover`/`.join:hover`.
    - Icônes Remixicon ajoutées (`ri-login-circle-fill`/`ri-user-add-fill`) sur demande explicite. Sur mobile (<=700px), passées en icône seule (bouton rond, texte en accessible-only `sr-only`, même technique que la navbar principale) plutôt qu'un mode mobile complet façon Musique/Histoire/Planning (refus explicite de l'utilisatrice : correctif ciblé sur le chevauchement constaté, pas de refonte).
    - `/register` plantait en 500 (`Cannot use the "format" option of DateType when the "html5" option is enabled`) : `widget` par défaut de `DateType` passé à `single_text` dans une version de Symfony plus récente que celle où ce formulaire a été écrit (le code suppose `choice`, cf. l'option `years` qui n'a de sens que pour ce widget). `widget: 'choice'` explicité dans `RegistrationType`, corrige sans changer le rendu (3 menus déroulants jour/mois/année, comportement d'origine).

## Phase 6 : Pages par rôle

- [ ] Même passage (code + UX), page par page, pour les vues spécifiques à chaque rôle (admin, comptable, membre, binioufous...)
- [x] Bugs 500 (`DateType`/`html5` sur `/desk/profile` et `/admin/user/{slug}`, `/register` sans photo), `ROLE_Simple` → `ROLE_SIMPLE`, icônes Font Awesome (jamais fonctionnelles) → Remix Icon, 1ère passe accessibilité/contraste sur `desk/`/`admin/`/`event/` (2026-08-11, branche `phase6_roles`). Détail dans l'historique de commits de la branche.
- [x] **Retour utilisatrice du 2026-08-11, traité le jour même** (même branche `phase6_roles`) :
    - `.card` (fiches `admin/user/show|addadmin|addaccountant.html.twig`) : fond blanc Bootstrap ("dégueulasse" selon l'utilisatrice, seule surface neutre de toute la zone) remplacé par un panneau encre (`--bb-ink`) + liseré doré (`body.desk .card`/`.card-body`, `assets/main/app.css`), plutôt qu'un simple ravalement de couleur.
    - Scrollbar de tout le desk/admin/event thématisée en doré pâle sur transparent (`body.desk`/`::-webkit-scrollbar-*`), même logique que Musique/Histoire/Planning.
    - Sélecteur de langue de `.navbar-admin` : `<select>` natif remplacé par les mêmes liens `.lang-link` que la navbar publique (adapté en blanc/rouge plutôt que doré/vert pour le contraste sur ce fond).
    - Dates du planning disparues : cause du `doctrine:fixtures:load` (purge sans recréation d'`Event`) confirmée. Les 13 dates de la saison 2026-2027 communiquées par l'utilisatrice sont désormais dans `AppFixtures.php` (survivent à un futur rechargement), répétitions à 09:17-12:34. Reste un lieu à préciser pour "Ze Big Journée de Rentrée" (mis en placeholder "À préciser" dans les fixtures, pas inventé).
    - Titres : plus un seul niveau différent par page. Un seul `<h1>` par page desk/admin/event (avant : mélange `h1`/`h3`/`h4`), sous-titres de section en `h2`, tous stylés avec la police d'affichage de la marque (Bungee, chargée sur les pages publiques mais jamais reprise ici jusque-là) plutôt que la police système par défaut (`body.desk h1`/`h2`, `assets/main/app.css`).
    - Boutons : icônes seules (modifier/valider/refuser/retirer un rôle dans les tableaux) forcées à une taille carrée fixe (`body.desk .table .btn-red/.btn-green`) pour ne plus varier selon le contenu des colonnes voisines.
    - `admin/unvalids.html.twig` : action "Refuser" ajoutée à côté de "Valider" (icône seule + confirmation JS, même pattern CSRF/method-override que la suppression d'événement). Décision prise faute de retour explicite de l'utilisatrice : refuser = supprimer le compte en attente (jamais validé, pas de raison de garder un compte "fantôme"), route `user_refuse` (`AdminController`).
    - Navbar admin fixe en haut au scroll (`position: fixed`), **desktop seulement (≥768px)** : sous ce seuil elle peut déjà occuper plusieurs lignes (reflow mobile du 2026-08-11), la fixer en plus aurait demandé de réserver une hauteur de contenu imprévisible en haut de chaque page. `#main-content` récupère un `padding-top` équivalent en desktop pour ne rien cacher au chargement.
    - Revérifié en mobile (375px) après coup : rien de cassé sur `/desk`, la fiche utilisateur et la liste de validation. Les tableaux (`desk/lists/*.twig`, `admin/unvalids.html.twig`) débordent horizontalement sur petit écran avec un scroll dédié, mais c'était déjà le cas avant cette passe (tables non responsive de base), pas une régression introduite ici ; vraie refonte de ces tableaux pas faite (hors scope de cette passe).
    - **Pas de traduction bretonne** pour les 3 nouvelles clés `admin.refuse*` (`translations/messages.br.yaml`), même raison que les autres lots de nouvelles clés déjà notés ailleurs dans ce fichier : pas de locuteur pour valider, tombe en repli français plutôt que d'inventer.
- [x] **Retour utilisatrice du 2026-08-11 (suite), 2 bugs sur `/desk`** :
    - **Colonne "Actions" pas alignée entre les tableaux** : la ligne 102 ci-dessus avait corrigé la taille des boutons eux-mêmes, mais pas la position de la colonne. `templates/desk/lists/*.twig` sont 5 `<table>` séparées (admins/comptables/binioufous/membres/simples) : en layout auto (par défaut), chaque table calcule ses largeurs de colonne indépendamment selon son propre contenu, donc "Actions" ne tombait pas à la même position d'une table à l'autre, même à colonnes identiques. Cause aggravante : la table "Simples utilisateurs" avait en plus une colonne "Pays" que les 4 autres n'avaient pas. Colonne "Pays" ajoutée aux 4 autres (`User::$country` existe déjà pour tout le monde, pas juste les simples), puis `table-layout: fixed` + mêmes largeurs en `%` sur les 6 colonnes communes (`body.desk .table`, `assets/main/app.css`) : alignement réel colonne par colonne désormais, plus une coïncidence liée au contenu.
    - **Section "Les Binioufous" affichée deux fois** sur `/desk` pour un compte qui cumule plusieurs rôles (ex. `ROLE_ADMIN` + `ROLE_BINIOUFOUS`, comme le compte `admin@admin.com` des fixtures) : `templates/desk/index.html.twig` bouclait sur `app.user.roles` et évaluait un `if/elseif` à *chaque* rôle du compte, donc `ROLE_ADMIN` (qui affiche déjà la liste des binioufous parmi ses 5 tableaux) ET `ROLE_BINIOUFOUS` déclenchaient chacun leur branche. Remplacé par un seul `if/elseif` basé sur `is_granted()`, évalué une fois, priorité décroissante (admin > binioufous > membre > simple) : une seule section rendue, celle du rôle le plus haut.
    - Vérifié en Playwright avec le compte admin multi-rôles (celui qui déclenchait les 2 bugs) : colonne Actions alignée sur les 5 tableaux, plus de doublon "Voici les Binioufous !" en bas de page.

## Avant les nouvelles fonctionnalités

- [ ] Emails fonctionnels : `symfony/mailer` est configuré (`config/packages/mailer.yaml`) mais `MAILER_DSN` est commenté dans `.env`, rien n'est réellement envoyé. Les formulaires de contact (`templates/story/index.html.twig` #contactForm, `templates/story/minisite.html.twig` #minisite-contact-form) ont des boutons `type="button"` : aucun des deux ne soumet quoi que ce soit, `story.js` se contente d'afficher/masquer le formulaire. Important malgré tout (pas juste un "nice to have"), à faire avant la Phase 7.
- [x] Composant popup de connexion générique et réutilisable (`templates/components/form-dialog.html.twig`, doc dans `doc/components.md`) : structure `role="dialog"`/`aria-modal`/`aria-labelledby`/bouton fermer accessible mutualisée, contenu du formulaire injecté en paramètre. Adopté sur `/join` (remplace les 2 `<form>` ad hoc) et sur `/music` (nouvelle popup de connexion directe, fragment générique `join/_login-generic.html.twig`, plus besoin de renvoyer vers `/join`).
- [x] Flux d'inscription clarifié (repéré par l'utilisatrice : "on sait pas trop où s'inscrire", le bouton cotisation HelloAsso et le choix `wish` étaient toujours visibles en même temps sans ordre clair ; direction validée le 2026-08-10, implémentée le 2026-08-11). Nouvelle question "Es-tu déjà adhérent·e ?" en tête de `/register`, dans une carte à part détachée visuellement du reste du formulaire (fond `--bb-ink` + liseré doré, toggle Oui/Non en boutons pilule façon `.login`/`.join`) : 1er jet posé dans le flux normal du formulaire (radios nues du navigateur) jugé "pas beau et pas clair" par l'utilisatrice, reconstruit en conséquence.
    - **Oui** : champ `User::$memberCardNumber` (nullable, migration `Version20260811190000`) apparaît, vérifié manuellement par un·e admin sur `/admin/{wish}/{slug}/valid` (colonne ajoutée au tableau existant).
    - **Non** : bouton cotisation HelloAsso mis en avant + souhait (`wish`) réellement verrouillé sur "Un grand fan !!" (`Simple`) en JS (options "Binioufous"/"Member" désactivées dans le `<select>`, réactivées si la personne revient sur "Oui") : un 1er jet qui se contentait d'un texte d'aide à côté du bouton, sans rien changer au formulaire, jugé insuffisant par l'utilisatrice ("ya pas de choix dans le formulaire, c juste un grand fan qu'il y a").
    - **3 bugs préexistants trouvés en construisant cette carte, sans rapport direct mais visibles sur cette même page** : (1) `gender`/`birth`/`wish` dans `RegistrationType` passaient le résultat de `getConfiguration()` en 4e argument de `add()` au lieu de le fusionner dans le tableau d'options (3e argument) ; `FormBuilderInterface::add()` n'accepte que 3 paramètres, un 4e est silencieusement ignoré par PHP (pas d'erreur), donc label/placeholder traduits jamais appliqués : ces 3 champs affichaient leur nom auto-humanisé en anglais ("Gender"/"Birth"/"Wish") au lieu de la traduction française attendue. Corrigé pour ces 3 champs. (2) La règle générique `form { color: var(--bb-gold) }` sur fond `--bb-green-mid` (3.63:1, sous AA) n'avait été corrigée que pour `body.desk form` (2026-08-11 plus tôt) : jamais traitée sur `/register`, tous les labels du formulaire (Prénom, Email...) étaient sous le seuil. Même correctif (blanc, 6.82:1), scopé à `.registration-form`. (3) `.badge-red` (bouton "Payer une cotisation", seul usage de cette classe) : `--bb-gold` sur `--bb-red` ne tenait que 3.24:1, sous AA. Blanc (6.07:1, AA), même correctif déjà appliqué à `#joinForm`/`#loginForm` pour la même paire de couleurs.
- [x] Page `/login` cassée (pas de vraie balise `<form>` autour des champs) : résolue autrement que prévu le 2026-08-10, cf. entrée "Audit accessibilité global" ci-dessous. `/join` (formulaire "normal") n'a jamais eu ce problème : `join/login.html.twig` a toujours été entouré d'un vrai `<form>` côté `join/index.html.twig`, seul le rendu standalone à `/login` en manquait.

## Phase 7 : Nouvelles fonctionnalités

- [ ] Une par une, sur une base stabilisée
- [x] Import/rendu de Markdown pour le texte de la page Histoire (branche `phase7_story_markdown`), plutôt que codé en dur dans `minisite.html.twig`/`translations/*.yaml`. Éditable par ROLE_ADMIN uniquement (décidé le 2026-08-12). Entité `StorySection` (titre, slug, contenu Markdown, position), CRUD `/admin/story` : nombre de sections et ordre libres, réordonnancement par boutons monter/descendre plutôt qu'un glisser-déposer (non atteignable au clavier, même choix que le gestionnaire de fichiers). Rendu via `twig/markdown-extra` + `league/commonmark` (extension GFM pour le strikethrough, `~~texte~~` remplace les `<s>` en dur d'origine).
    - Éditeur : EasyMDE (barre d'outils réduite au strict nécessaire, aperçu affiché en permanence à côté plutôt qu'à activer), pour rester simple à des rédacteur·ices non-informaticien·nes plutôt qu'un textarea Markdown brut.
    - Contenu existant (Intro/Qui/Quoi/Pourquoi) repris dans une migration écrite à la main (`doctrine:migrations` toujours cassé sur cette base, cf. Phase 5) et dans `AppFixtures.php` (`StorySectionSeedData`) pour survivre à un futur `fixtures:load`.
    - Français uniquement pour l'instant (décidé avec l'utilisatrice) : en/br gardent le titre de page et la note mobile, mais plus le contenu des 4 sections d'origine, les clés `story.*` correspondantes retirées des 3 fichiers de traduction.
    - Nav "façon terminal" (`@intro`, `@cki ?`...) abandonnée au profit du titre complet de chaque section dans le menu d'ancres : un seul champ à remplir par section plutôt que titre + label de nav séparés.
    - Retour utilisatrice le jour même : colonne de texte du terminal trop étroite sur grand écran (beaucoup d'espace vide à gauche/à droite), `main` élargi (`max-width` 720px → 1000px, padding latéral 1.25rem → 3rem, `assets/story/minisite.css`).
- [x] "Facilitons l'inscription" (branche `inscription2.0`, décidé le 2026-08-12) : `/register` réduit à pseudo/email/mot de passe (`RegistrationType`), tout le reste (identité, instrument, adhésion...) déménagé sur `/desk/profile`, facultatif (`User::$firstName/$lastName/$gender/$birth/$city/$country/$instrument` passés `nullable: true`, migration à la main comme d'habitude sur cette base, `doctrine:migrations` toujours cassé cf. Phase 5). `User::$slug`/`getFullName()` basés sur le pseudo plutôt que prénom/nom (vides à la création désormais).
    - **Connexion jamais bloquée**, même avant validation admin : 1er jet (`UserChecker`, bloquait la connexion elle-même) retiré le jour même sur retour utilisatrice ("même pas validé, on doit pouvoir accéder à son profil"). `/desk` affiche à la place un bandeau "compte en cours de vérification" + lien direct vers `/desk/profile` tant que `User::$validation` est `false` (`desk.pending_review`/`desk.pending_review_cta`), tout le reste de `/desk` reste normalement accessible. Chaque inscription attend quand même cette validation pour être visible dans les listes/recevoir un rôle : plus d'auto-validation "Simple" comme avant (le champ qui la déclenchait, `wish`, est justement celui qu'on retire, cf. plus bas).
    - **Bug trouvé par l'utilisatrice en testant** : un compte tout juste validé par un·e admin n'apparaissait dans aucune liste `/desk` (ni Simples, ni Binioufous), invisible malgré un accès déjà fonctionnel : `wish` ne déclenchant plus d'attribution de rôle automatique, plus rien n'en donnait un du tout. `AdminController::validUser()` attribue désormais `ROLE_SIMPLE` par défaut à la validation (seulement si le compte n'a encore aucun rôle métier), palier de base cohérent avec le toggle Membre/Pas membre qui prend le relai ensuite.
    - **Emails squelettes** (demande explicite : "mets le squelette, le reste on verra après") : un mail part vers l'équipe à chaque inscription (`LoginController::notifyAdminsOfNewRegistration()`), un autre vers la personne une fois son compte accepté (`AdminController::notifyUserOfAcceptance()`). `MAILER_DSN` toujours pas configuré (cf. item "Emails fonctionnels" plus bas, bloqué sans accès mail) : `parameters: env(MAILER_DSN): 'null://null'` posé dans `config/services.yaml` en valeur de repli, sinon la seule injection de `MailerInterface` (avant même un `send()`) fait planter tout le conteneur (`EnvNotFoundException`). Adresse admin (`admin_notification_email`, même fichier) en placeholder `contact@binioufous.fr` à remplacer par la vraie adresse.
    - **Rôles simplifiés** : `wish` (souhait choisi à l'inscription, `Binioufous`/`Member`/`Simple`) retiré entièrement (colonne, route `/admin/{wish}/{slug}/valid` devenue `/admin/{slug}/valid`, tri par wish de la liste d'attente remplacé par la date d'inscription). Il déterminait avant à la fois le rôle attribué à la validation ET si celle-ci était automatique ; ces deux choses sont maintenant décorrélées. `ROLE_MEMBER` confirmé ne débloquer aucune permission propre dans le code (absent de `security.yaml`) lors de la discussion avec l'utilisatrice : nouveau toggle `ROLE_SIMPLE ↔ ROLE_BINIOUFOUS` (seul rôle avec une vraie différence fonctionnelle, accès aux partitions/voix) en un clic depuis les listes `/desk` (`AdminController::toggleMembership()`, boutons dans `desk/lists/simples.html.twig`/`binioufous.html.twig`), décorrélé de la validation du compte elle-même. `ROLE_MEMBER` reste en base pour les comptes qui l'avaient déjà, plus jamais attribué par ce nouveau mécanisme.
    - **Rôles fusionnés** (même jour, suite directe du point précédent) : `ROLE_SIMPLE` retiré à son tour, fusionné avec `ROLE_USER`. Il ne servait qu'à marquer "compte validé, sans `ROLE_BINIOUFOUS`", un état déjà couvert par `ROLE_USER` (attribué à tout compte connecté directement en code, `User::getRoles()`, jamais stocké en base) : autant traiter "simple" comme l'absence de `ROLE_BINIOUFOUS` plutôt que comme un rôle à assigner/retirer explicitement. `AdminController::validUser()` n'attribue plus rien à la validation (le bug qui avait fait ajouter ce palier, cf. point précédent, ne se pose plus : `UserRepository::findSimples()` n'a plus besoin d'un rôle explicite pour retrouver ces comptes). `AdminController::toggleMembership()` n'ajoute/retire plus que `ROLE_BINIOUFOUS`. Bouton de promotion "Simple" (`create_simple`) et clés de traduction associées (`admin.make_simple`/`admin.confirm_make_simple`/`Simple` fr+br) retirés. Migration `Version20260812170000` : `DELETE FROM role WHERE title = 'ROLE_SIMPLE'` (cascade sur `role_user` via la contrainte déjà en place) pour nettoyer les comptes de prod qui l'avaient déjà, contrairement à `ROLE_MEMBER` laissé stale intentionnellement (pas de valeur historique/associative à `ROLE_SIMPLE`, c'était un pur détail d'implémentation).
    - `User::$memberCardNumber` (numéro de carte) : "plus besoin du numéro" (retour utilisatrice), laissé en base pour ne pas perdre les données déjà saisies mais retiré de tous les formulaires. Remplacé par `User::$claimsMembership` (booléen, déclaration facultative "Oui, il me semble" adhérent·e sur le profil, purement informatif, ne donne aucun accès automatiquement).
    - Bugs trouvés et corrigés en même temps (accès à des champs maintenant facultatifs sans filet) : `UserRepository::findUnvalids()` triait par `wish` (colonne retirée, plantait) ; plusieurs templates (`admin/user/show.html.twig`, `admin/user/valid.html.twig`, `admin/unvalids.html.twig`, les 5 listes `desk/lists/*.twig`) affichaient `user.instrument.title`/`user.birth|date(...)` sans vérifier que ces champs ne sont plus toujours renseignés, plantait dès le premier compte inscrit via le nouveau flux minimal.
    - Page `/desk/profile` mise à jour : carte "Es-tu déjà adhérent·e ?" simplifiée ("Oui, il me semble"/"Non", `User::$claimsMembership`, sans numéro de carte), champ "Autre" en texte libre pour l'instrument (`User::$otherInstrumentDetail`, entrée "Autre" ajoutée à la table `Instrument`), tous les champs facultatifs avec `required: false` explicite côté formulaire (`AccountType`/`EditUserType`, mêmes champs en miroir côté admin). Rappel "renseigne ton instrument" en flash lors de l'ajout d'une voix en favori sans instrument défini (`DeskController::toggleVoice()`).
    - **En cours** : passe design + UX complète de `/desk/profile` (photo à gauche, infos à droite, sections en dessous) et clarté côté admin, confiée à l'agent dédié `accessibilite` (même pattern que la refonte de `/register`).
- [ ] Remplacer les usages de Google :
    - [x] Espace adhérent avec accès aux morceaux (avec la possibilité de choisir ses parties favorites, comme ça on peut aussi retrouver globalement qui joue quelle partie)
    - [ ] Espace administratif pour les membres du conseil et du bureau avec les CR, les documents administratifs, un outil de prise de note en ligne (mais pas nécessairement collaboratif) : le stockage de fichiers existe (cf. ci-dessous), reste l'outil de prise de note
    - [ ] Espace compta avec un accès aux relevés uploadés, la possibilité de produire des devis et des factures et de faire le suivi de trésorerie (journal des dépenses et des recettes) : le stockage de fichiers existe (cf. ci-dessous), reste devis/factures/trésorerie
    - [ ] Dans l'espace adhérent, un service de covoiturage pour les dates éloignées
- [x] Gestionnaire de fichiers façon Drive (branche `phase7_parties`) généralisé en 3 espaces cloisonnés par rôle, sous le nouvel onglet navbar "Dossiers" (`/desk/files`, remplace l'ancien lien unique "Morceaux") : **Musique** (`ROLE_BINIOUFOUS`, contient aussi le tas morceaux/voix favorites déjà existant), **Administratif** (`ROLE_ADMIN`) et **Comptabilité** (`ROLE_COMPTA`), `ROLE_ADMIN` ayant accès aux 3. Un seul jeu d'entités (`Folder`/`Document`) déjà en place (commit `c36ad43`, upload glisser-déposer, suppression, visionneuse, favoris) partagé entre les 3 : `Folder::$space` (dénormalisé, hérité du parent à la création) sépare les arborescences, une racine réelle par espace créée à la volée au premier accès (`FolderRepository::findOrCreateRoot()`) plutôt que `folder_id`/`parent_id` NULL implicite comme avant. Migration `Version20260811180000` : reparente l'arborescence musique préexistante sous cette nouvelle racine, `document.folder_id` devient obligatoire.
    - Déplacement de fichier/dossier ajouté (`document_move`/`folder_move`) : pas de `<select>` ni de glisser-déposer (accessibilité clavier, `<select>` déjà écarté ailleurs sur ce projet pour cause de chrome non stylable) mais un mode navigation dédié (`?move_document=`/`?move_folder=`) qui réutilise l'arborescence déjà affichée, avec un bouton "Déplacer ici" par dossier traversé. Anti-cycle côté serveur (`FolderRepository::isSelfOrDescendantOf()`) pour refuser de déplacer un dossier dans lui-même ou un de ses sous-dossiers.
    - Testé en Playwright (compte admin multi-rôles + compte binioufous seul créé/retiré ponctuellement pour l'occasion) : hub filtré par rôle, 403 confirmé sur `/desk/files/admin` et `/desk/files/accounting` pour un compte binioufous seul, upload, déplacement (y compris cycle refusé), suppression.
- [x] Retour utilisatrice du 2026-08-11 sur ce gestionnaire de fichiers, traité le jour même (recherche des patterns WAI-ARIA concernés avant d'implémenter, cf. sources ci-dessous) :
    - **Navbar desk/admin cassée sur mobile** ("la navbar en mode tel... la aled") : les 8 items + sélecteur de langue s'empilaient en 1 colonne remplissant tout l'écran avant même le contenu de la page (`.navbar-admin` n'avait ni bouton hamburger ni panneau repliable, juste un `flex-wrap` qui empilait au lieu de déborder). Refaite avec un vrai bouton hamburger + panneau repliable (pattern WAI-ARIA "Disclosure Navigation Menu"), en réutilisant tel quel le mécanisme déjà en place et déjà audité pour la navbar publique (`.navbar-toggler`, `aria-expanded`/`aria-controls`, fermeture Escape) plutôt que d'en inventer un 2e : `assets/main/app.js` généralisé pour lire le `data-target` du bouton au lieu d'un id `#navbarText` en dur, afin de marcher sur les 2 navbars sans dupliquer le JS. Desktop (≥768px) inchangé (barre horizontale fixe).
    - **Lien "Dossiers" toujours via le hub, même avec un seul espace accessible** ("tout le monde voit... même quand y'a qu'un qui est autorisé, c'est chelou") : un membre qui n'a accès qu'à un seul espace (cas fréquent, ex. Binioufous seul) passait quand même par `/desk/files` pour un choix qui n'en était pas un. Le lien de la navbar pointe désormais directement vers l'unique espace accessible quand il n'y en a qu'un, et vers le hub seulement à partir de 2.
    - **Création de dossier** ajoutée (`FolderController::create()`, formulaire compact toujours visible plutôt qu'un bouton à déplier) : permet de préparer une destination avant un déplacement de fichier, ce qui manquait jusqu'ici. Doublon de nom refusé explicitement (pas de fusion silencieuse).
    - **Fil d'Ariane** mis en conformité avec le pattern WAI-ARIA "Breadcrumb" (`<ol>`/`<li>` + `aria-current="page"` sur le maillon courant, qui n'est plus un lien vers lui-même).
    - Confirmé que déplacer un fichier/dossier **hors** d'un dossier (vers un ancêtre, jusqu'à la racine) fonctionnait déjà via la navigation du fil d'Ariane en mode déplacement (les liens d'ancêtres gardent `?move_document=`/`?move_folder=` dans l'URL) : pas de code supplémentaire nécessaire, juste vérifié en conditions réelles suite à une question de l'utilisatrice.
    - Sources consultées : [WAI-ARIA APG – Breadcrumb Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/), [WAI-ARIA APG – Disclosure Navigation Menu Example](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/examples/disclosure-navigation/), [WCAG G65 – Providing a breadcrumb trail](https://www.w3.org/WAI/WCAG21/Techniques/general/G65.html).
- [x] 2e retour utilisatrice le même jour ("comment c'est moche et pas lisible l'ajout de dossier") : passe design + accessibilité complète sur toute la section Dossiers, confiée à l'agent dédié `accessibilite` du projet plutôt que traitée à la main.
    - **Cause racine du formulaire "Nouveau dossier" moche/peu lisible** : `.desk-new-folder` (un simple `<form>`) héritait sans le vouloir de la règle générique `form { background: --bb-green-mid; border: 5px solid --bb-border-dark; padding: 2% 4%; }` définie tout en haut d'`app.css` pour les formulaires classiques (profil, validation...). Le label en `--bb-ink` par-dessus ne tenait que **2.70:1** sur ce fond vert (calcul de luminance relative, pas à l'œil), sous le minimum AA. Refait en carte dédiée alignée sur `.quick-upload-zone` juste à côté (bordure `--bb-green-mid` pleine plutôt que pointillée, pour distinguer "champ à remplir" de "zone à déposer"), label en `--bb-ink` sur le doré du body = 9.81:1 (AAA).
    - **Hiérarchie de lecture** : "Nouveau dossier" et la zone de dépôt regroupés dans un wrapper `.desk-actions` (grille 2 colonnes à poids égal dès 640px, empilé en dessous) au lieu de l'ancien enchaînement vertical où "Nouveau dossier" (une fine ligne) était écrasé sous la grande zone de dépôt.
    - **Panneau mobile de `.navbar-admin`** (retour complémentaire, "faut peut-être enlever la barre rouge moche derrière" à propos du menu hamburger ouvert) : le panneau `#navbarAdminNav` héritait du même `--bb-red` que la barre du haut, ce qui donnait un aplat rouge uniforme du haut de l'écran jusqu'à "Déconnexion", sans relief. Passé en `--bb-red-dark` + coins arrondis + ombre, pour se détacher visuellement de la barre plutôt que de la prolonger. Contraste texte encore amélioré au passage (8.56:1 au lieu de 6.07:1). Comportement (hamburger, `aria-expanded`, Escape, cible tactile 44px) inchangé.
    - **Cohérence globale** : nouvelle variable partagée `--bb-shadow-soft` (`:root`) appliquée à toute la "famille carte" du gestionnaire de fichiers (formulaire nouveau dossier, zone de dépôt, bannière de déplacement, cartes du hub, panneau mobile de la navbar) : un seul traitement de relief au lieu d'aplats de couleur juxtaposés sans lien visuel entre eux.
    - **Cibles tactiles mobiles** (≤576px) : étoile favori, boutons déplacer/supprimer (dossier et document), pastilles de voix passés à 44px (ils étaient entre 28 et 36px). Cartes du hub en pleine largeur sous 400px.
    - Vérifié après coup en Playwright (mobile 375px et desktop 1400px) : panneau mobile détaché visuellement de la barre du haut, formulaire "Nouveau dossier" lisible et fonctionnel (création confirmée), disposition côte à côte cohérente sur desktop, hub avec le même relief que le reste.

## Déjà fait

- [x] CI sécurité JS (`npm audit` + `dependency-review-action`)
- [x] Migration Webpack Encore 0.30 → 7.2, `node-sass` → `sass`
- [x] Node pinné à `24` via `.nvmrc`
- [x] Nettoyage fichiers obsolètes (backups d'éditeur, `old.cpp`, `.env` retiré du tracking git)
- [x] Convention de commit (gitmoji + français, template `.gitmessage`)
- [x] Crédits déplacés dans `.github/CONTRIBUTING.md` (emplacement standard GitHub, plus de `CREDITS.md` à la racine) : cousin de Guillaume à identifier, aucune trace trouvée dans git ni dans les fichiers `.gltf`
- [x] CI cassée depuis le 2026-08-06 (job `lint-php-twig`, check des traductions) : `DATABASE_URL` factice ajoutée au `.env` éphémère du job, le kernel Symfony refusait de booter sans (le retrait de `.env` du tracking git avait cassé cette étape sans que personne ne le remarque, `composer-audit`/`npm-audit`/`deploy` ne tournaient donc plus sur les push depuis 4 jours)
- [x] Ménage CSS (`assets/main/app.css`, `music.css`, `story.css`) : toutes les couleurs de marque en variables `--bb-*` (quasiment aucune ne l'était), CSS mort supprimé (`.device-orientation`, `.bg-red`, `.bg-green`, `.bg-red-light`, `.yellow-hover`, jamais référencés dans aucun template/JS), et plusieurs bugs de contraste rouge/vert du même type que celui corrigé sur la navbar (texte illisible ou invisible) trouvés et corrigés ailleurs : boutons `.btn-red`/`.btn-green` (desk/admin), pills actives du planning et de la minisite histoire, `.red-hover`/`.green-hover` (page adhésion)
